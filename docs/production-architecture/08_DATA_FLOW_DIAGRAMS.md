# Data Flow Diagrams
## Visual Representations of System Interactions

**Document Version:** 1.0  
**Last Updated:** January 13, 2026  
**Reading Time:** 15 minutes  
**Status:** ✅ Current

---

## 🎯 **Purpose**

This document provides visual diagrams showing how data flows through the system for critical operations. Use these to understand component interactions and identify potential bottlenecks.

---

## 📊 **1. User Authentication Flow**

```
┌─────────────┐
│   Browser   │
└──────┬──────┘
       │ 1. POST /api/auth/login
       │ { email, password }
       │
       ▼
┌─────────────────────────────────────────┐
│         API Gateway (Nginx)              │
│  • Rate Limit Check (5 req/min)         │
│  • CORS Validation                       │
└──────────────┬──────────────────────────┘
               │ 2. Forward request
               ▼
┌─────────────────────────────────────────┐
│      AuthController (.NET API)           │
│  • Extract email & password              │
└──────────────┬──────────────────────────┘
               │ 3. FindByEmailAsync(email)
               ▼
┌─────────────────────────────────────────┐
│      ASP.NET Core Identity               │
│  • Query AspNetUsers table               │
└──────────────┬──────────────────────────┘
               │ 4. User found
               ▼
┌─────────────────────────────────────────┐
│         BCrypt Password Verify           │
│  • Compare hashed password               │
└──────────────┬──────────────────────────┘
               │ 5. Password valid
               ▼
┌─────────────────────────────────────────┐
│       JWT Token Generation               │
│  • Create claims (UserId, Email, Role,   │
│    SchoolId)                             │
│  • Sign with secret key                  │
│  • Set 3-hour expiry                     │
└──────────────┬──────────────────────────┘
               │ 6. Generate refresh token
               ▼
┌─────────────────────────────────────────┐
│         SQL Server Database              │
│  • Insert into RefreshTokens table       │
└──────────────┬──────────────────────────┘
               │ 7. Tokens ready
               ▼
┌─────────────────────────────────────────┐
│        HTTP Response                     │
│  • 200 OK                                │
│  • Body: { accessToken, user }           │
│  • Cookie: refreshToken (httpOnly)       │
└──────────────┬──────────────────────────┘
               │ 8. Store token in memory
               ▼
┌─────────────┐
│   Browser   │
│  (Logged In)│
└─────────────┘
```

**Key Points:**
- Rate limiting prevents brute force (Layer 1)
- Password never stored in plain text (BCrypt hash)
- JWT contains SchoolId claim (critical for multi-tenancy)
- Refresh token in httpOnly cookie (XSS protection)

---

## 📊 **2. Student Creation Flow (Multi-Tenant)**

```
┌─────────────┐
│   Teacher   │
│  (School A) │
└──────┬──────┘
       │ 1. POST /api/students
       │ Headers: Authorization: Bearer <JWT>
       │ Body: { firstName, lastName, classId, ... }
       │
       ▼
┌─────────────────────────────────────────┐
│    Authentication Middleware             │
│  • Validate JWT signature                │
│  • Check expiration                      │
│  • Extract claims                        │
└──────────────┬──────────────────────────┘
               │ 2. JWT valid
               │ Claims: { schoolId: "AAA-111", role: "Teacher" }
               ▼
┌─────────────────────────────────────────┐
│   SchoolIsolationMiddleware ⭐           │
│  • Extract SchoolId from claims          │
│  • Validate SchoolId != Guid.Empty       │
│  • Store in HttpContext.Items            │
└──────────────┬──────────────────────────┘
               │ 3. SchoolId validated
               ▼
┌─────────────────────────────────────────┐
│     Authorization Middleware             │
│  • Check role: Teacher or Admin          │
└──────────────┬──────────────────────────┘
               │ 4. Authorized
               ▼
┌─────────────────────────────────────────┐
│      StudentController                   │
│  • GetUserSchoolId() = "AAA-111"         │
│  • Validate ClassId belongs to School A  │
└──────────────┬──────────────────────────┘
               │ 5. Call service
               ▼
┌─────────────────────────────────────────┐
│       StudentService                     │
│  • FluentValidation (name, DOB, etc.)    │
│  • Generate admission number             │
│  • Set SchoolId = "AAA-111"              │
└──────────────┬──────────────────────────┘
               │ 6. Call repository
               ▼
┌─────────────────────────────────────────┐
│      StudentRepository                   │
│  • Map DTO to Entity                     │
│  • dbContext.Students.Add(student)       │
└──────────────┬──────────────────────────┘
               │ 7. Execute SQL
               ▼
┌─────────────────────────────────────────┐
│       SQL Server Database                │
│  INSERT INTO Students                    │
│  (Id, SchoolId, FirstName, ...)          │
│  VALUES                                  │
│  (newGuid, 'AAA-111', 'Raj', ...)        │
│                                          │
│  Index used: IX_Students_SchoolId        │
└──────────────┬──────────────────────────┘
               │ 8. Record inserted
               ▼
┌─────────────────────────────────────────┐
│        Audit Logging                     │
│  • Log: "Teacher X created Student Y     │
│    in School A"                          │
└──────────────┬──────────────────────────┘
               │ 9. Success response
               ▼
┌─────────────┐
│   Teacher   │
│ (201 Created)│
└─────────────┘
```

**Security Layers:**
1. JWT validation (who are you?)
2. SchoolId isolation (your school only)
3. Role authorization (can teachers create students?)
4. Resource validation (does ClassId belong to your school?)

---

## 📊 **3. Cross-School Access Attempt (BLOCKED)**

```
┌─────────────┐
│   Teacher   │
│  (School A) │
│ Malicious   │
└──────┬──────┘
       │ 1. GET /api/students/STUDENT_FROM_SCHOOL_B
       │ Headers: Authorization: Bearer <JWT_SCHOOL_A>
       │
       ▼
┌─────────────────────────────────────────┐
│    Authentication Middleware             │
│  • JWT valid                             │
│  • Claims: { schoolId: "AAA-111" }       │
└──────────────┬──────────────────────────┘
               │ 2. Authenticated
               ▼
┌─────────────────────────────────────────┐
│      StudentController                   │
│  • GetUserSchoolId() = "AAA-111"         │
│  • GetStudentById(STUDENT_FROM_SCHOOL_B) │
└──────────────┬──────────────────────────┘
               │ 3. Query database
               ▼
┌─────────────────────────────────────────┐
│       SQL Server Database                │
│  SELECT * FROM Students                  │
│  WHERE Id = 'STUDENT_FROM_SCHOOL_B'      │
│  AND SchoolId = 'AAA-111' ⭐             │
│                                          │
│  Result: 0 rows (student is in School B)│
└──────────────┬──────────────────────────┘
               │ 4. Not found
               ▼
┌─────────────────────────────────────────┐
│      StudentController                   │
│  • if (student == null)                  │
│    return NotFound()                     │
└──────────────┬──────────────────────────┘
               │ 5. OR check ownership
               ▼
┌─────────────────────────────────────────┐
│   ValidateSchoolOwnership()              │
│  • if (student.SchoolId != userSchoolId) │
│    Log security incident                 │
│    return Forbid()                       │
└──────────────┬──────────────────────────┘
               │ 6. Log security event
               ▼
┌─────────────────────────────────────────┐
│        Audit Logging                     │
│  • Log: "SECURITY: Teacher from School A │
│    attempted access to School B student" │
│  • Severity: Critical                    │
│  • Alert SuperAdmin                      │
└──────────────┬──────────────────────────┘
               │ 7. Error response
               ▼
┌─────────────┐
│   Teacher   │
│ (403 Forbid)│
│  ❌ Blocked │
└─────────────┘
```

**Defense Layers:**
- SQL WHERE clause ALWAYS filters by SchoolId
- Controller validates ownership
- Audit logs security incidents
- SuperAdmin alerted immediately

---

## 📊 **4. SuperAdmin Access Flow (With Audit)**

```
┌─────────────┐
│ SuperAdmin  │
└──────┬──────┘
       │ 1. GET /api/schools/SCHOOL_B/students
       │ Headers: Authorization: Bearer <SUPERADMIN_JWT>
       │
       ▼
┌─────────────────────────────────────────┐
│    Authentication Middleware             │
│  • JWT valid                             │
│  • Claims: { role: "SuperAdmin" }        │
│  • No SchoolId claim (SuperAdmin)        │
└──────────────┬──────────────────────────┘
               │ 2. Authenticated
               ▼
┌─────────────────────────────────────────┐
│   SchoolIsolationMiddleware              │
│  • Check role == "SuperAdmin"            │
│  • if (SuperAdmin)                       │
│    Log access to AuditLog ⭐             │
│    Allow bypass                          │
└──────────────┬──────────────────────────┘
               │ 3. Bypass allowed (logged)
               ▼
┌─────────────────────────────────────────┐
│      StudentController                   │
│  • if (User.IsInRole("SuperAdmin"))      │
│    Use provided SchoolId parameter       │
│  • else                                  │
│    Use user's SchoolId from claims       │
└──────────────┬──────────────────────────┘
               │ 4. Query with School B ID
               ▼
┌─────────────────────────────────────────┐
│       SQL Server Database                │
│  SELECT * FROM Students                  │
│  WHERE SchoolId = 'BBB-222' (School B)   │
│                                          │
│  Result: All School B students           │
└──────────────┬──────────────────────────┘
               │ 5. Students retrieved
               ▼
┌─────────────────────────────────────────┐
│        Audit Logging ⭐                  │
│  INSERT INTO AuditLogs                   │
│  (UserId, Action, SchoolId, Timestamp)   │
│  VALUES                                  │
│  (SuperAdminId, 'AccessedSchoolData',    │
│   'BBB-222', NOW())                      │
│                                          │
│  • Email alert to compliance officer     │
└──────────────┬──────────────────────────┘
               │ 6. Success response
               ▼
┌─────────────┐
│ SuperAdmin  │
│ (200 OK)    │
│ ✅ + Logged │
└─────────────┘
```

**Key Points:**
- SuperAdmin can bypass school isolation
- EVERY access is logged in AuditLogs
- Alerts sent for suspicious activity
- Audit logs immutable (append-only)

---

## 📊 **5. File Upload Flow (Cloud Storage)**

```
┌─────────────┐
│   Teacher   │
└──────┬──────┘
       │ 1. POST /api/files/upload
       │ Content-Type: multipart/form-data
       │ Body: file (binary) + metadata
       │
       ▼
┌─────────────────────────────────────────┐
│      FileController                      │
│  • Validate file type (PDF, DOCX, JPG)   │
│  • Validate file size (< 10MB)           │
│  • Extract SchoolId from JWT             │
└──────────────┬──────────────────────────┘
               │ 2. File valid
               ▼
┌─────────────────────────────────────────┐
│       File Storage Service               │
│  • Generate unique filename              │
│    school-AAA-111/students/file-xyz.pdf  │
│  • Encrypt file (AES-256)                │
└──────────────┬──────────────────────────┘
               │ 3. Upload to cloud
               ▼
┌─────────────────────────────────────────┐
│    Azure Blob Storage / AWS S3           │
│  Container: school-management-files      │
│  Path: /AAA-111/students/file-xyz.pdf    │
│                                          │
│  • Server-side encryption enabled        │
│  • Access control: Private               │
└──────────────┬──────────────────────────┘
               │ 4. Upload complete
               │ URL: https://storage.../file-xyz.pdf
               ▼
┌─────────────────────────────────────────┐
│       SQL Server Database                │
│  INSERT INTO Files                       │
│  (Id, SchoolId, FileName, FileUrl,       │
│   UploadedBy, EntityType, EntityId)      │
└──────────────┬──────────────────────────┘
               │ 5. Metadata saved
               ▼
┌─────────────┐
│   Teacher   │
│ { fileId,   │
│   fileUrl } │
└─────────────┘
```

**Security:**
- File path includes SchoolId (isolation)
- Files encrypted at rest
- Private access (signed URLs for downloads)
- Virus scanning (future enhancement)

---

## 📊 **6. Real-Time Chat Flow (SignalR)**

```
┌─────────────┐                    ┌─────────────┐
│  Teacher    │                    │   Parent    │
│ (School A)  │                    │ (School A)  │
└──────┬──────┘                    └──────┬──────┘
       │                                  │
       │ 1. Connect to SignalR Hub        │ 2. Connect to SignalR Hub
       │ WebSocket: /chatHub              │ WebSocket: /chatHub
       │ Auth: JWT in query string        │ Auth: JWT in query string
       │                                  │
       ▼                                  ▼
┌──────────────────────────────────────────────────┐
│            SignalR ChatHub (.NET)                 │
│  • OnConnectedAsync()                            │
│  • Validate JWT                                  │
│  • Extract SchoolId from claims                  │
│  • Store connectionId → userId mapping in Redis  │
└──────────────┬───────────────────────────────────┘
               │ 3. Both connected
               │
       ┌───────┴───────┐
       │               │
       ▼               ▼
┌─────────────┐   ┌─────────────┐
│  Teacher    │   │   Parent    │
│ConnectionID │   │ConnectionID │
│  abc-123    │   │  xyz-789    │
└──────┬──────┘   └──────┬──────┘
       │                 │
       │ 4. SendMessage  │
       │ { roomId, text }│
       ▼                 │
┌──────────────────────────────────────────────────┐
│            ChatHub.SendMessage()                  │
│  • Validate user is member of room               │
│  • Validate room belongs to user's school        │
│  • Encrypt message                               │
└──────────────┬───────────────────────────────────┘
               │ 5. Save to database
               ▼
┌─────────────────────────────────────────────────┐
│       SQL Server Database                        │
│  INSERT INTO ChatMessages                        │
│  (RoomId, SenderId, Message, SentAt)             │
└──────────────┬──────────────────────────────────┘
               │ 6. Message saved
               ▼
┌─────────────────────────────────────────────────┐
│       SignalR Hub                                │
│  • Get all connectionIds in room                 │
│  • Send message to all connections               │
│    Clients.Group(roomId).SendAsync(...)          │
└──────────────┬──────────────────────────────────┘
               │ 7. Broadcast to room
       ┌───────┴───────┐
       │               │
       ▼               ▼
┌─────────────┐   ┌─────────────┐
│  Teacher    │   │   Parent    │
│ ✅ Receives │   │ ✅ Receives │
│   message   │   │   message   │
└─────────────┘   └─────────────┘
```

**Features:**
- Real-time bidirectional communication
- School isolation enforced (can't join other school's rooms)
- Messages encrypted before storage
- Offline users get messages on next login

---

## 📊 **7. Caching Strategy Flow**

```
┌─────────────┐
│   Request   │
│ GET /api/   │
│ schools/123 │
└──────┬──────┘
       │
       ▼
┌─────────────────────────────────────────┐
│      SchoolController                    │
│  • Check cache first                     │
└──────────────┬──────────────────────────┘
               │ 1. Cache key: "school:123"
               ▼
┌─────────────────────────────────────────┐
│          Redis Cache                     │
│  • GET "school:123"                      │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
       ▼ Cache HIT     ▼ Cache MISS
┌─────────────┐   ┌─────────────────────────┐
│Return cached│   │Query SQL Server Database│
│    data     │   │SELECT * FROM Schools    │
│             │   │WHERE Id = '123'         │
│(Fast: 5ms)  │   │                         │
└─────────────┘   └──────────┬──────────────┘
                             │ 2. Data retrieved
                             ▼
                  ┌─────────────────────────┐
                  │Store in Redis Cache     │
                  │SET "school:123" data    │
                  │EXPIRE 300 (5 minutes)   │
                  └──────────┬──────────────┘
                             │ 3. Return data
                             ▼
                  ┌─────────────────────────┐
                  │Response to client       │
                  │(Slower: 50ms first time)│
                  └─────────────────────────┘
```

**Cache Invalidation:**
```
┌─────────────┐
│   Request   │
│ PUT /api/   │
│ schools/123 │
└──────┬──────┘
       │ Update school data
       ▼
┌─────────────────────────────────────────┐
│      SchoolService.UpdateAsync()         │
│  • Update database                       │
│  • Invalidate cache                      │
└──────────────┬──────────────────────────┘
               │ 1. Update DB
               ▼
┌─────────────────────────────────────────┐
│       SQL Server Database                │
│  UPDATE Schools SET ... WHERE Id = '123' │
└──────────────┬──────────────────────────┘
               │ 2. DB updated
               ▼
┌─────────────────────────────────────────┐
│          Redis Cache                     │
│  • DEL "school:123"                      │
│  • Cache invalidated                     │
└──────────────┬──────────────────────────┘
               │ 3. Next GET will re-cache
               ▼
         (Cache Miss → Query DB → Cache)
```

---

## 📊 **8. Bulk Import Flow (CSV)**

```
┌─────────────┐
│    Admin    │
└──────┬──────┘
       │ 1. Upload students.csv (500 rows)
       │ POST /api/students/bulk-import
       │
       ▼
┌─────────────────────────────────────────┐
│      StudentController                   │
│  • Read CSV file                         │
│  • Parse rows (500 students)             │
└──────────────┬──────────────────────────┘
               │ 2. Validate each row
               ▼
┌─────────────────────────────────────────┐
│       FluentValidation                   │
│  • Row 1: ✅ Valid                       │
│  • Row 2: ✅ Valid                       │
│  • Row 50: ❌ Invalid email              │
│  • Row 100: ❌ Missing DOB               │
│  • Row 450: ✅ Valid                     │
│                                          │
│  Result: 498 valid, 2 errors             │
└──────────────┬──────────────────────────┘
               │ 3. Return validation report
               ▼
┌─────────────┐
│    Admin    │
│ Fix 2 rows  │
│ Re-upload   │
└──────┬──────┘
       │ 4. Re-upload (all valid now)
       ▼
┌─────────────────────────────────────────┐
│      StudentService                      │
│  • Batch insert (100 rows at a time)     │
└──────────────┬──────────────────────────┘
               │ 5. Batch 1 (rows 1-100)
               ▼
┌─────────────────────────────────────────┐
│       SQL Server Database                │
│  BEGIN TRANSACTION                       │
│  INSERT INTO Students (100 rows)         │
│  COMMIT                                  │
└──────────────┬──────────────────────────┘
               │ 6. Batch 1 complete
               │ Repeat for batches 2-5
               ▼
┌─────────────────────────────────────────┐
│      Background Job (Send Credentials)   │
│  • Queue 500 email jobs                  │
│  • Send login credentials to parents     │
└──────────────┬──────────────────────────┘
               │ 7. Import complete
               ▼
┌─────────────┐
│    Admin    │
│"500 students│
│ imported"   │
└─────────────┘
```

**Performance:**
- Batch inserts (100 at a time) instead of 500 individual INSERTs
- Background job for email sending (don't block HTTP response)
- Transaction per batch (rollback if batch fails)

---

## 📚 **Next Steps**

1. **Deployment:** [09_DEPLOYMENT_ARCHITECTURE.md](./09_DEPLOYMENT_ARCHITECTURE.md)
2. **Performance:** [13_PERFORMANCE_OPTIMIZATION.md](./13_PERFORMANCE_OPTIMIZATION.md)
3. **Monitoring:** [17_MONITORING_LOGGING.md](./17_MONITORING_LOGGING.md)

---

**Document Status:** ✅ Complete  
**Last Updated:** January 13, 2026
